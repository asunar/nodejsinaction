function setup(format) {
  var regexp = /:(\w+)/g;

  return function configurable_logger(req, res, next) {
    var str = format.replace(regexp, function(match, property){
      return req[property];
    });

    console.log("Configured log: " + str);

    next();
  }
}

module.exports = setup;
